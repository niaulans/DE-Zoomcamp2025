Create ssh keys:
ssh-keygen -t rsa -f ~/.ssh/KEY_FILENAME -C USERNAME
ssh-keygen -t rsa -f ssh/gcp -C niaulans

Change permissions:
chmod 600 ssh/gcp

Connect to GCP instance:
ssh -i ~/.ssh/KEY_FILENAME USERNAME@EXTERNAL_IP
ssh -i ssh/gcp niaulans@34.87.35.29

htop -> get information about CPU, RAM, and disk usage

Docker without sudo:
sudo groupadd docker
sudo gpasswd -a $USER docker
sudo service docker restart
logout
login again

chmod +x docker-compose -> make it executable

Make docker compose can access in anywhere:
ex: in bin/
then export PATH="${HOME}/bin:${PATH}"

export GOOGLE_APPLICATION_CREDENTIALS=creds/key.json -> to access GCP

PROJECT_NAME="taxi-rides-ny"
gcloud auth application-default set-quota-project ${PROJECT_NAME}

Unset variable linux:
